# Project dependencies
# Add your dependencies here, e.g.:
# requests==2.31.0
hyperon>=0.2.0
uagents>=0.20.1 # Latest compatible with Python 3.9
openai>=1.0.0
python-dotenv
uagents-core>=0.4.0
chromadb
sentence-transformers

# MCP Server
mcp

# Benchmarking
selenium
webdriver-manager
tqdm

# Unibase Membase (Decentralized Memory Layer)
# Install separately: pip install git+https://github.com/unibaseio/membase.git
# Or clone: git clone https://github.com/unibaseio/membase.git && pip install -e membase/
